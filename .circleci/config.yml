version: 2.1

orbs:
  node: circleci/node@1.1.6
  heroku: circleci/heroku@0.0.10
  eslint: arrai/eslint@2.0.0

jobs:
  build:
    executor:
      name: node/default
    steps:
    - checkout
    - run: npm install
  test:
    executor:
      name: node/default
    steps:
    - checkout
    - run: npm run test
    - run: npm run lint
  deploy:
    executor:
      name: heroku/default
    steps:
      - checkout
      - heroku/deploy-via-git
workflows:
  main:
    jobs:
      - build
      - test:
          requires:
            - build
      - deploy:
          requires:
            - build