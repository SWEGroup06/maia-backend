version: 2.1

jobs:
  build-and-test:
    docker:
      - image: circleci/python:3.8.2
    steps:
      - checkout
      - run: pip install -r requirements.txt
      - run: python3 src/test.py # Move to test directory

workflows:
  main:
    jobs:
      - build-and-test